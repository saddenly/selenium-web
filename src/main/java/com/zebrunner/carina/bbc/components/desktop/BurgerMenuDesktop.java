package com.zebrunner.carina.bbc.components.desktop;

import com.zebrunner.carina.bbc.components.BurgerMenu;
import org.openqa.selenium.SearchContext;
import org.openqa.selenium.WebDriver;

public class BurgerMenuDesktop extends BurgerMenu {
    public BurgerMenuDesktop(WebDriver driver, SearchContext searchContext) {
        super(driver, searchContext);
    }
}
